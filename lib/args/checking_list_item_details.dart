class CheckingListItemDetails{

  final title;
  final itemModel;
  final ownerName;
  final invoiceNo;
  final checkingFee;
  final createdDate;

  CheckingListItemDetails(this.itemModel, this.ownerName, this.invoiceNo, this.checkingFee, this.createdDate, this.title);
}